{
  "$id": "{{singularResourceName}}.json",
  "type": "object",
  "required": [],
  "properties": {
    {{#each properties}}
    "{{propertyName}}": { "type": "{{propertyType}}" },
    {{/each}}
  }
}
