{
  "$id": "{{singularResourceName}}.json",
  "type": "object",
  "required": [],
  "properties": {
    {{#each properties}}
    "{{propertyName}}": { "type": "{{propertyType}}" }{{#unless @last}},{{/unless}}
    {{/each}}
  }
}
